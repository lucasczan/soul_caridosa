import styled from 'styled-components/native';

export const Container = styled.SafeAreaView``;

export const AddHomeless = styled.TouchableOpacity`
  border: ${(props) => props.border};
  border-radius: 20;
  align-items: center;
  margin-top: 10;
`;
